# AoE II Stuff

Here you can find the following:
1. An operational NAND gate (`NAND-scripted-final.aoe2scenario`)
2. An operational perceptron (`Perceptron.aoe2scenario`)
3. The circuit (non-operational atm) for training the perceptron (`Perceptron_training.aoe2scenario`)

AoE II:DE was used to generate these scenarios. We cannot guarantee they load in any other version.
